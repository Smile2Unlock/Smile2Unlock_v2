*.bin
compile_commands.json
.codex
Cargo.lock
!src/core-rs/Cargo.lock
!src/platform/windows/credential_provider_rs/Cargo.lock
target/
zig-cache/
.zig-cache/

# vscode
.history/


# Folder
build
vsxmake2022
.xmake
.vscode
.idea
.vs
.cache
.claude

CMakeLists.txt
# 安装器文件
installer-files
Output

# Release signing material must live in the GitHub release-signing environment,
# never in the repository or build artifacts.
release-signing/
*.key
*.pem
*.p12
*.pfx

.*
!.github/
!.github/workflows/
!.github/workflows/*.yml
# The credential provider crate needs its committed cargo config: it carries
# the wine test runner required to execute mingw test binaries on Linux.
!src/platform/windows/credential_provider_rs/.cargo/
!src/platform/windows/credential_provider_rs/.cargo/config.toml
