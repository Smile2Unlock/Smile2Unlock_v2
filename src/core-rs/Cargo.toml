[package]
name = "su_core"
version = "0.1.0"
edition = "2024"

[lib]
name = "su_core"
crate-type = ["staticlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
toml = "0.9"
tracing = "0.1"
zeroize = "1"

