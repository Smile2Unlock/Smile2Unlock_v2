// Application icon embedded into the su_app.exe resource section
// (Explorer file icon, and the default window/taskbar icon until the
// runtime WM_SETICON call applies the same icon at window creation).
IDI_APP_ICON ICON "../../assets/icons/Smile2Unlock.ico"
